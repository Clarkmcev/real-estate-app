<div class="container">
    <main class="mt-5 pt-4">
        <div class="container dark-grey-text mt-5 px-4 bg-light bg-opacity-50">
            <div class="row wow fadeIn">
                <div class="col-md-6 mb-4">
                    <img src="/images/user-icon.png" class="img-fluid">
                </div>

                <div class="col-md-6 mb-4">
                    <div class="p-4">
                        <div class="mb-3">
                            <p class="text font-italic"></p>
                            <h2 class="pt-3">{{user.username}}</h2>
                        </div>
                    </div>
                    {{#each user.reviews}}
                    <lu>
                        <li>{{this.comment}} -by {{this.user.username}}</li>
                    </lu>
                    {{/each}}

                        <form action="/page/{{user.id}}" method="POST">
                        <label> Write a review
                        <input type="text" name="comment"/>
                        </label>
                        <button type="submit">Submit review</button>
                    </form>
                </div>

            </div>
        </div>
    </main>
</div>